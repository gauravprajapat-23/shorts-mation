import {describe,expect,it} from "vitest";import {readFile} from "node:fs/promises";
describe("campaign native worker wiring",()=>{it("has no active Shotstack calls",async()=>{for(const f of ["src/lib/render-pipeline.server.ts","src/lib/render-settings.functions.ts","src/lib/render-settings.server.ts","src/routes/_app/settings.tsx"]){const s=await readFile(f,"utf8");expect(s.toLowerCase()).not.toContain("shotstack");}});it("keeps browser rendering explicit",async()=>{expect(await readFile("src/routes/_app/campaigns/$campaignId.test-render.tsx","utf8")).toContain("attachBrowserRenderedOutput");});});
